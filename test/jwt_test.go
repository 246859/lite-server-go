package test

import (
	"testing"
)

func TestJwt(t *testing.T) {
	//secret := "abcdefg"
	//hs256Jwt, err := jwtutils.CreateHs256Jwt(jwtutils.UserClaims{UserId: "a", UserUUID: "a"}, secret, "a", time.Hour)
	//fmt.Println(hs256Jwt, err)
	//fmt.Println(jwtutils.ParseHs256Jwt(hs256Jwt, secret))
}
